[meta]
schema_version = 1
profile_version = "1.0.0"

[model]
type = "llamacpp"
id = "marlin-smoke-test-tiny"
status = "untested"
notes = "Synthetic profile for exercising 'marlin profile pull' (schema_version <= supported). Points at a nonexistent gguf_path deliberately — this is for testing the pull/checksum/decode/write mechanics only, not for actually starting a server."

[serve]
gguf_path = "/nonexistent/smoke-test.gguf"
ngl = 0
context_size = 2048
