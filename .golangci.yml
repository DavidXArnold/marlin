version: "2"

run:
  timeout: 5m

linters:
  enable:
  - errcheck
  - govet
  - staticcheck
  - ineffassign
  - misspell
  - noctx
  - bodyclose
  exclusions:
    rules:
    - path: _test\.go
      linters:
      - errcheck
