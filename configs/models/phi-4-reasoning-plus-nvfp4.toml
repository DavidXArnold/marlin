[model]
id     = "nvidia/Phi-4-reasoning-plus-NVFP4"
type   = "vllm"
status = "untested"
notes  = "Phi-4 reasoning+ 14B in NVFP4 precision. --quantization is omitted; vLLM auto-detects from model config."

[serve]
quantization           = "nvfp4"
gpu_memory_utilization = 0.90
max_model_len          = 32768
served_model_name      = ["local", "phi-4-reasoning-plus"]
